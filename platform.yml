project: platform
prefix: platform
migrations: db/migrations
stack:
  - rust
  - terraform
rust_artifacts:
  lambdas:
    - auth-trigger
    - ci-ingest
    - cors-handler
    - db-migrate
    - db-migrate-truenas
    - komodo-proxy
    - og-server
